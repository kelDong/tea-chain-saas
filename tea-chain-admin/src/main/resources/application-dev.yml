server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tea_chain_saas?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  redis:
    host: localhost
    port: 6379

logging:
  level:
    com.teachain: debug
    org.springframework.security: debug
